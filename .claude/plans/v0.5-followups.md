# v0.5 followups

Items deferred from the 2026-05-05 merge wave (5 unmerged branches consolidated
into v0.4-refactor). Each item below has been considered and intentionally
deferred to v0.5; do not silently re-litigate during normal v0.4.x work.

## 1. Hard-reject `costs_type='log' + demand_adjustment=True`

**Status: CLOSED in v0.4 final** (Phase 2 of multi-column endog work,
2026-05-08). Resolved by Option (b): implement the proper chain rule
rather than reject. ``gradient_markups`` is rescaled by
``f'(p − Δ_m) = 1/(p − Δ_m)`` so the demand-adjusted variance reflects
the log-cost moment derivative. Both ``demand_results`` and
``demand_params`` paths support the combination. The
``fix/log-costs-with-demand-adjustment`` branch is retired. See
``.claude/plans/multi-column-endog-cost-transforms.md`` Phase 2.

## 2. Add no-cost-formulation support to `Problem.__init__`

**Status (v0.4):** `Problem.__init__` raises `TypeError` at
[pyRVtest/problem.py:1178](../../pyRVtest/problem.py:1178) if
`cost_formulation` isn't a `Formulation`. There is no path to omit cost
shifters entirely.

**Followup branch:** `origin/testing` (Anya Tarascina, Oct 2023). The branch
adds `if self.products.w.any():` guards but on pre-v0.4 file paths
(`pyRVtest/economies/problem.py`), so it cannot be cherry-picked. The branch
also bundles a separate, **incorrect** change to the marginal-cost formula
that does not match DMSS (2024) eq (3) under rule-of-thumb pricing
(η = (φ-1)c → c = (νp - τ) / (1 + ν · cost_scaling)); v0.4-refactor's formula
is correct and must not be touched. **Branch deleted.**

**v0.5 work:** add `cost_formulation: Optional[Formulation] = None` to
`Problem.__init__`. When `None`, skip the OLS residualization steps in
`_compute_instrument_results` (the `sm.OLS(..., self.products.w).fit()`
calls). Add a regression test against an expected-equivalent path that
passes `Formulation('0')` or similar. Estimated effort: ~1 day.

## 3. Add the `demand_adjustment + endogenous_cost_component` gate

**Status: CLOSED in v0.4 final via Option (b).** Step 4d/4e of the v0.4
unified-demand-adjustment refactor (commit ``8bc0df4``) added
``_compute_gamma_gradient`` (finite-diff over the IV correction at
perturbed demand parameters); the resulting ``∂γ_m / ∂θ`` channel is
folded into the test_engine variance term at line 413. Cross-path
parity ``demand_results`` vs ``demand_params`` is pinned to
``atol=5e-9`` by
``tests/test_demand_adjustment.py::test_option_a_demand_params_matches_demand_results_with_endogenous_cost``.
Stale FAQ caveats and the ``test_memo1_section42_adjustment_interaction.py``
xfail-strict regression test were cleaned up on 2026-05-08
(commit ``9cceefd``).

## 4. Rewrite the §4.1 F-stat rank-adjustment regression test

**Status (v0.4):** the original test at
`tests/regressions/test_memo1_section41_f_diagnostic.py` was deleted during
the 2026-05-05 merge wave because its design (compare F_A / F_B against
K/(K-1)) degenerated to 0/0 on the `tiny_synthetic` fixture. Whether the
underlying K-1 fix is in place remains untested at this layer.

**v0.5 work:** rewrite the test against a fixture rich enough to produce
non-zero F-stats. Could lift from `tests/replication/test_dmss_yogurt.py`
or a downsampled variant. Then verify the K-1 adjustment by inspecting
`F_cv_size_list` row indices directly rather than computing F ratios.

## 5. F-stat collapse under identically-zero-markup candidate models

**Symptom:** in the README quick-start output (`pyRVtest.data.load_example`,
PC truth, 4 candidate models including PerfectCompetition), every F-stat
cell involving model 3 (PC) is suspiciously small — F = 0.0, 1.4, or 2.6
— even though pairwise TRVs involving PC are sharply significant
(``***``). For example, the (Bertrand, PC) pair shows ``TRV = 6.771***``
but ``F = 2.6``, and the (Cournot, PC) pair shows ``TRV = 0.419`` (the
Dearing-degenerate cell, expected) with ``F = 0.0``.

**Hypothesis:** when one model in a pair has identically-zero markup
(``PerfectCompetition``), the residual variance for that model collapses,
the σ-cross terms vanish, and the F denominator structure degenerates
even when the moment-condition difference is well-defined. The TRV
statistic and MCS p-values look correct in this regime; only the F-stat
diagnostic appears off.

**Where to look:** F-stat computation in
`pyRVtest/solve/test_engine.py` around the σ̂_0, σ̂_1, σ̂_2 trace
contractions and the F numerator/denominator assembly (search for
``F_numerator`` and ``F_denominator``).

**Decision needed:** is this a real bug to fix, or an inherent property
of the F-stat formula at the zero-markup boundary that should be
documented as a known limitation? The TRV / MCS outputs are clean, so
the F-stat is the only signal that misleads — possible to live with
either way.

**Discovered:** 2026-05-05 docs Phase 1 session, while writing the
README quick-start narrative for the shipped synthetic example.

## 6. Rewrite notebook tutorials to v0.4 class-based API + re-execute

**Status: COMPLETED 2026-05-06.** Both notebooks are now v0.4-API-clean
and re-executed; saved outputs match the input code.

What landed:

- `docs/notebooks/testing_firm_conduct.ipynb` (Nevo cereal + PyBLP
  demand) — Bertrand / Cournot / Monopoly / PerfectCompetition all
  use the class-based API. The two Vertical entries use
  `pyRVtest.Vertical(downstream=..., upstream=...)`. The tax test
  moved tax columns to the Problem level and uses the per-model
  `unit_tax_salient` / `advalorem_tax_salient` opt-outs to reproduce
  the pre-v0.4 behavior of "model 1 has tax, models 2 & 3 do not."
- `docs/notebooks/model_library.ipynb` — every model class uses its
  v0.4 form (Bertrand, Cournot, Monopoly, PartialCollusion for
  Bertrand-with-kappa, Cournot with kappa via the standard Cournot
  class, PerfectCompetition, RuleOfThumb wrapper, ConstantMarkup,
  Vertical, CustomConductModel with callable, UserSuppliedMarkups).
- v0.3 deprecation banner cells removed from both notebooks; the
  notebooks now reflect the v0.4 API end-to-end.
- Environment fix bundled in: pyblp upgraded 1.1.0 → 1.2.0 to land in
  the `numpy>=2 + pyblp>=1.2` lane CI exercises. Older pyblp
  1.1.0 + numpy 2.3.4 had `np.unicode_` removal, blocking notebook
  execution; that's resolved.

Closing commit: `a283c48` ("DOC: rewrite cereal-data + model-library
notebooks to v0.4 API").
