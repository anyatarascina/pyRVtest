formats:
    - htmlzip
    - pdf

build:
    image: latest

sphinx:
   configuration: docs/conf.py

python:
    requirements: requirements.txt
    version: 3.7
    pip_install: true
    extra_requirements:
        - docs