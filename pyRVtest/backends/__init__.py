"""Demand / supply backend protocols and implementations.

This subpackage is a skeleton as of v0.4 step 1 (module layout). The
DemandBackend protocol and PyBLPBackend, LogitBackend, NestedLogitBackend,
UserSuppliedBackend implementations land in step 3. The LaborSupplyBackend
skeleton lands in step 14.

See `.claude/plans/v0.4-refactor.md` §4.1 for the module layout and §3
for the PyBLPBackend prototype that this subpackage will host.
"""

__all__: list[str] = []
